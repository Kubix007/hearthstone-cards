import { Box } from "@mui/material";
import styled from "styled-components";

export const Container = styled(Box)`
  position: absolute;
  top: 85px;
  left: 38px;
  filter: drop-shadow(black 0px 0px 0.55rem);
`;

export const HeroPowerImage = styled.img`
  height: 180px;
  width: 130px;
`;
